{
    "description": "config for plot_graph.py",
    "properties": {
        "graphviz": {
            "type": "object",
            "properties": {
                "oredering": {
                    "enum": ["in", "out"]
                },
                "rankdir": {
                    "enum": ["LR", "RL", "TB", "BT"]
                }
            }
        }
    }
}
